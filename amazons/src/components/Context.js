// Import Component
import { createContext } from "react";

// Variables for the Player
export const player_context = createContext({})
export const player_id = createContext({})

// Variables for the enviroment

export const language = createContext({})